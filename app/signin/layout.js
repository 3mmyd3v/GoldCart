import "@styles/globals.css";
export const metadata = {
  title: "Authorization | SignIn",
  description: "SignIn",
};

export default function RootLayout({ children }) {
  return <section>{children}</section>;
}
